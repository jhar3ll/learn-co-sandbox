<div class="container">
      <h1> Welcome, <%=current_user.username.capitalize()%>!</h1>
    </div> 
<h2> Here Are Your Games! </h2>

<h3> Games: </h3>
<% @games.each do |game| %>
  <div>
    <%= game.content %>
  </div>
<% end %>